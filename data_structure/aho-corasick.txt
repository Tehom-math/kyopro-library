// https://youtu.be/BYoRvdgI5EU
struct Aho{
  using MP=unordered_map<char,int>;
  vector<MP> to;
  vector<int> cnt, fail;
  Aho() : to(1), cnt(1){}
  int add(const string &s){
    int v=0;
    for(auto c:s){
      if(!to[v].count(c)){
        to[v][c]=to.size();
        to.push_back(MP());
        cnt.push_back(0);
      }
      v=to[v][c];
    }
    cnt[v]++;
    return v;
  }
  void init(){
    fail=vector<int>(to.size(),-1);
    queue<int> que;
    que.push(0);
    while(!que.empty()){
      auto v=que.front();
      que.pop();
      for(auto [c,nv]:to[v]){
        fail[nv]=(*this)(fail[v],c);
        cnt[nv]+=cnt[fail[nv]];
        que.push(nv);
      }
    }
  }
  int operator()(int v,char c) const{
    while(v != -1){
      auto it=to[v].find(c);
      if(it != to[v].end()) return it->second;
      v=fail[v];
    }
    return 0;
  }
  int operator[](int v) const{return cnt[v];}
};